---
import { getSchoolContacts } from '../../../lib/school.ts';
import { normalizeSocialUrl } from '../../../lib/social.ts';
import { getSocialIconPath } from '../_shared/social-icons';
interface Props { school: any; [key: string]: any; }
const { school } = Astro.props;
const contacts = getSchoolContacts(school.id);
const social = (school.socialHandles as Record<string, string>) || {};
const socialEntries = Object.entries(social).filter(([, v]) => v && v !== '#');
---
<div class="mv-feed space-y-4">
  <section class="mv-grad rounded-[28px] p-6">
    <p class="text-white/80 text-xs font-semibold uppercase tracking-[0.16em]">Get in touch</p>
    <h1 class="text-white text-2xl font-extrabold mt-1 leading-tight">Contact us</h1>
  </section>

  {contacts.length > 0 && (
    <section class="mv-card p-5">
      <h2 class="text-sm font-bold mb-3" style="color:var(--text)">Contact info</h2>
      <ul class="space-y-3">
        {contacts.map(c => <li><p class="text-[10px] font-bold uppercase tracking-wider" style="color:var(--primary)">{c.label || 'Contact'}</p><p class="text-sm" style="color:var(--text)">{c.value}</p></li>)}
      </ul>
      {socialEntries.length > 0 && (
        <div class="flex items-center gap-2 mt-4 pt-4 border-t" style="border-color:color-mix(in srgb, var(--border) 50%, transparent)">
          {socialEntries.map(([p,h]) => { const path = getSocialIconPath(p); if (!path) return null; return <a href={normalizeSocialUrl(h,p)} target="_blank" rel="noopener noreferrer" aria-label={`Visit our ${p} page`} class="mv-soft p-2.5 rounded-full mv-tap"><svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24" style="color:var(--text)"><path d={path} /></svg></a>; })}
        </div>
      )}
    </section>
  )}

  <section class="mv-card p-5">
    <h2 class="text-sm font-bold mb-3" style="color:var(--text)">Send a message</h2>
    <form id="contactForm" class="space-y-3">
      <input type="text" name="name" required placeholder="Name" class="w-full px-4 py-3 rounded-xl text-sm outline-none border mv-tap" style="background:var(--bg);border-color:var(--border);color:var(--text)" />
      <input type="email" name="email" required placeholder="Email" class="w-full px-4 py-3 rounded-xl text-sm outline-none border mv-tap" style="background:var(--bg);border-color:var(--border);color:var(--text)" />
      <input type="tel" name="phone" placeholder="Phone" class="w-full px-4 py-3 rounded-xl text-sm outline-none border mv-tap" style="background:var(--bg);border-color:var(--border);color:var(--text)" />
      <input type="text" name="subject" required placeholder="Subject" class="w-full px-4 py-3 rounded-xl text-sm outline-none border mv-tap" style="background:var(--bg);border-color:var(--border);color:var(--text)" />
      <textarea name="message" rows="4" required placeholder="Message" class="w-full px-4 py-3 rounded-xl text-sm outline-none border mv-tap" style="background:var(--bg);border-color:var(--border);color:var(--text)"></textarea>
      <div id="contactFormMessage" class="hidden"></div>
      <button type="submit" class="mv-grad text-white w-full py-3.5 rounded-xl text-sm font-bold mv-tap">Send Message</button>
    </form>
  </section>
</div>

<script is:inline define:vars={{ schoolSlug: school.slug }}>
  document.getElementById('contactForm')?.addEventListener('submit', async function(e) {
    e.preventDefault();
    var msgEl = document.getElementById('contactFormMessage');
    var formData = new FormData(this); var data = {};
    formData.forEach(function(val, key) { data[key] = val; });
    data.action = 'create_ticket';
    msgEl.className = 'p-3 rounded-xl text-sm'; msgEl.style.background = 'color-mix(in srgb, var(--primary) 10%, transparent)'; msgEl.style.color = 'var(--muted)'; msgEl.textContent = 'Sending...'; msgEl.classList.remove('hidden');
    try {
      var res = await fetch('/api/' + schoolSlug + '/support', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) });
      var result = await res.json();
      if (res.ok) { msgEl.style.color = 'var(--text)'; msgEl.innerHTML = '<strong>Sent!</strong> We will reply soon.'; this.reset(); }
      else { throw new Error(result.error || 'Failed'); }
    } catch(err) { msgEl.style.background = 'rgba(239,68,68,0.1)'; msgEl.style.color = '#dc2626'; msgEl.textContent = 'Failed: ' + (err.message || 'Unknown error'); }
  });
</script>
