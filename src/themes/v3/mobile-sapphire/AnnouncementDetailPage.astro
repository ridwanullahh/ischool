---
import { getSchoolNav, getSchoolContacts } from '../../../lib/school.ts';
import { normalizeSocialUrl } from '../../../lib/social.ts';
import { getSocialIconPath } from '../_shared/social-icons';

interface Props { school: any; [key: string]: any; }
const { school, ...data } = Astro.props;
const nav = getSchoolNav(school.id);
const contacts = getSchoolContacts(school.id);
const prefix = '/' + school.slug;
const social = (school.socialHandles as Record<string, string>) || {};
const socialEntries = Object.entries(social).filter(([, v]) => v && v !== '#');
const locale = school.locale || 'en';
const formatDate = (d: string | Date | null) => d ? new Date(d).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) : '';
---
<div class="px-4 py-16">
  <article class="max-w-3xl mx-auto">
    <a href={`/${school.slug}/announcements`} class="inline-flex items-center gap-1.5 text-sm mb-4 hover:opacity-70" style="color:var(--muted)">Back to Announcements</a>
    <div class="aurora-glass-strong rounded-3xl p-8 sm:p-12">
      {data.item?.isPinned && <span class="aurora-grad px-2 py-0.5 text-[10px] font-bold text-white rounded-full mb-4 inline-block">PINNED</span>}
      <h1 class="text-3xl sm:text-4xl font-bold mb-4" style="color:var(--text)">{data.item?.title || 'Announcement'}</h1>
      <p class="text-sm mb-6" style="color:var(--muted)">{formatDate(data.item?.publishedAt || data.item?.createdAt)}</p>
      <div class="prose max-w-none whitespace-pre-wrap" style="color:var(--text)">{data.item?.content || ''}</div>
    </div>
  </article>
</div>
